services:
  postgres:
    image: postgres:15-alpine
    container_name: lol-challenge-postgres
    environment:
      POSTGRES_USER: lolapp
      POSTGRES_PASSWORD: lolapp_dev
      POSTGRES_DB: lolchallenge
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U lolapp -d lolchallenge']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: lol-challenge-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
